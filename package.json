{
  "name": "dsh-feishu-bot",
  "version": "1.1.0",
  "description": "Feishu remote control for DeepSeek Harness: commands, approval cards, confirm cards, progress push. DSH plugin (spawns the bridge as a managed child).",
  "type": "module",
  "license": "MIT",
  "main": "lib/index.js",
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.40.0"
  }
}
